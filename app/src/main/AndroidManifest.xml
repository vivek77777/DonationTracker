<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.donationtracker">

    <!-- Permission to post notifications, required for Android 13 (API level 33) and above -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- MainActivity declaration -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DonationSummaryActivity declaration -->
        <activity android:name=".DonationSummaryActivity"
            android:exported="false" />

        <!-- DonationHistoryActivity declaration -->
        <activity android:name=".DonationHistoryActivity"
            android:exported="false" />

    </application>

</manifest>
